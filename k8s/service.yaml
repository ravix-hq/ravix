apiVersion: v1
kind: Service
metadata:
  name: switchyard
  namespace: switchyard
  labels:
    app: switchyard
spec:
  type: ClusterIP
  selector:
    app: switchyard
  ports:
    - port: 80
      targetPort: 8080
